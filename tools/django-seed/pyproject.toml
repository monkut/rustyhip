[project]
name = "django-seed"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
    "django-rustyhip",
]

[tool.uv.sources]
django-rustyhip = { path = "../../../django-rustyhip", editable = true }
